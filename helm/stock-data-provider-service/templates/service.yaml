apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock-data-provider-service.fullname" . }}
  labels:
    {{- include "stock-data-provider-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "stock-data-provider-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
